spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clog?characterEncoding=UTF-8
    username: root
    password: 1234

decorator:
  datasource:
    p6spy:
      enable-logging: true

kakao:
  map:
    rest-api-key: ${KAKAO_MAP_REST_API_KEY}
    local-search-base-url: ${KAKAO_MAP_LOCAL_SEARCH_BASE_URL}